spring.application.name=Survey

#Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD}

#JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

admin.username=${ADMIN_USER:eyedine_admin}
admin.password=${ADMIN_PASS:eyedine_survey30}
jwt.secret=${JWT_SECRET:SuperSecretCafeSurveyKey2026Secure!!}

spring.security.user.name=${admin.username}
spring.security.user.password=${admin.password}